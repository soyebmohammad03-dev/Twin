{
  "name": "twin-platform",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "dev:api": "npm run build:packages && npm run dev --workspace apps/api",
    "dev:worker": "npm run build:packages && npm run dev:worker --workspace apps/api",
    "build:packages": "npm run build --workspace packages/contracts --workspace packages/db",
    "build": "npm run build:packages && npm run build --workspace apps/api && npm run build --workspace apps/web",
    "typecheck": "npm run build:packages && npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:generate": "npm run db:generate --workspace packages/db",
    "db:migrate": "npm run db:migrate --workspace packages/db"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.2",
    "vitest": "^4.1.11"
  }
}
