# VITE_API_URL: Base URL of the Twin API (apps/api). Only VITE_-prefixed
# vars are exposed to client code by Vite. Defaults to
# http://localhost:4000 in code if unset, so this is optional for local dev.
VITE_API_URL="http://localhost:4000"
